apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "docmost-openviking-sync.fullname" . }}
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: {{ .Values.persistence.storageClassName | quote }}
  resources:
    requests:
      storage: {{ .Values.persistence.size | quote }}
